# Database Configuration
# For local development: postgresql://user:password@localhost:5432/bikeathon
DATABASE_URL="postgresql://<user>:<password>@localhost:<port>/bikeathon"
PRISMA_DATABASE_URL="postgres://<user>:<password>@localhost:<port>/template1?sslmode=disable&connection_limit=1&connect_timeout=0&max_idle_connection_lifetime=0&pool_timeout=0&single_use_connections=true&socket_timeout=0"
# Direct database URL for migrations (required for some providers with connection pooling)
# Uncomment if using Supabase, Neon, or other pooled connections
# DIRECT_URL="postgresql://user:password@localhost:5432/bikeathon"

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# App Configuration (Optional)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_CURRENCY=USD
